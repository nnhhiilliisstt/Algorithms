//find the absolute value of a number.

function absoluteValue(num) {
  // Find absolute value of `num`
  if (num < 0) {
    return -num;
  }
  // Executes if condition is not met
  return num;
}

// Run `absoluteValue` function to find absolute value of two numbers.

console.log(absoluteValue(-7689));
